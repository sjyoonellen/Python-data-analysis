import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import datetime as dt
import matplotlib.pyplot as plt


import warnings
warnings.filterwarnings("ignore")

df=pd.read_csv('C:\\Users\\sjyoo\\OneDrive\\바탕 화면\\transactions.csv')

duplicated_df = df[df.duplicated(subset=['transaction_id', 'user_id'], keep=False)]
duplicated_df_sorted = duplicated_df.sort_values(by=['transaction_id', 'user_id'])
